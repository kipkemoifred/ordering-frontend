import React from 'react';
import OrderHistory from '../components/OrderHistory';

const OrderPage = () => {
    return (
        <div>
            <OrderHistory />
        </div>
    );
};

export default OrderPage;
